The first one task1vol1 is to calculate for itself, it will output values when that process is activated
The second one task1vol2 is for specific process based on absolute path
Usage.csv is table of values
test.py is basic programm I tested it on
